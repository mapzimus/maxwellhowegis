# Build deps for scripts/build_interstate_data.py
# Only the standard library + requests is used at runtime; requests is
# optional (urllib is the actual transport). ortools is NOT needed —
# Version B uses a pure-Python local-search solver.
requests==2.32.3
# Needed by the screenshot scripts (capture_thumbs.py, screenshot_atlas.py,
# screenshot_interstate.py), which drive headless Chromium.
playwright==1.47.0
